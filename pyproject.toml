[project]
name = "gallery-server"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "flask>=3.0.3",
]

[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"
